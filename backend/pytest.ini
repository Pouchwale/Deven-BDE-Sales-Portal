[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    error
    # Starlette's own import of a deprecated anyio alias. Not ours to fix.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
