# Where the FastAPI backend is reachable from the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Shows the seeded-account shortcuts on the sign-in page. Leave unset in production.
NEXT_PUBLIC_SHOW_DEMO_HINT=false
